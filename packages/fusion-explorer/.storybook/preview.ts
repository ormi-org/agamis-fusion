// import "./default.css";
import { Meta } from '@storybook/angular';

const meta: Meta = {
    parameters: {
        options: {
            storySort: {
              order: ['Admin', 'Shared']
            }
        }
    }
}