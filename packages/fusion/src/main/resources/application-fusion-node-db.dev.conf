include "application-base.dev.conf"

fusion.core.db {
    ignite {
        nodes = [
            "apache-ignite-1",
            "apache-ignite-2"
        ]
        backups = 1
    }
    mongo {
        uri = "mongodb://mongo:mongo@mongodb:27017"
    }
}

